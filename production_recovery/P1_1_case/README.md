# P1_1 real channel one-way flexible-fibre case

This case uses the real Channel DNS input template from P1_0 and raises the guarded one-way validation grid to `nx=96`, `ny=97`, `nz=96`, with `dt=5.0e-5` and `ilast=100`. It is a short validation case only: lambda remains zero and no two-way feedback is enabled.
