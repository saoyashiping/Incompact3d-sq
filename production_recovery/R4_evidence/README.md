# R4 Evidence Directory

This directory stores R4 standalone IBM interpolation evidence. In this environment, R4 build/run evidence is blocked before executable generation because the requested MPI Fortran compiler/decomp2d path is unavailable.
