# R7 Evidence Directory

This directory stores R7 standalone FSI closed-loop micro-case evidence. In this environment, R7 build/run evidence is blocked before executable generation because the requested MPI Fortran compiler/decomp2d path is unavailable.
