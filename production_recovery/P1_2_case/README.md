# P1_2 real channel small-lambda two-way case

Real Channel DNS case for guarded P1_2 small-lambda two-way DNS-FSI response: 96x97x96, `dt=2.5e-5`, `ilast=150`, one 49-node fibre, and two runs with `lambda=1.0e-5` and `lambda=1.0e-4`. This is not production DNS.
