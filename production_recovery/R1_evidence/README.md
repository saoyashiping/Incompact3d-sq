# R1 Evidence Directory

This directory is reserved for R1 baseline build/run evidence and R1-only input copies.

No R1 input copy or runtime output was generated in this attempt because configure was blocked before an executable could be built.
