# R3 Evidence Directory

This directory stores R3 standalone grid-adapter evidence. In this environment, R3 build/run evidence is blocked before executable generation because no Fortran compiler is available.
