# R5 Evidence Directory

This directory stores R5 standalone dry structure solver evidence. In this environment, R5 build/run evidence is blocked before executable generation because the requested MPI Fortran compiler/decomp2d path is unavailable.
